DEXY
===============

Make an ERC20 wallet and do some transactions on a dex

Requirements
===============
python3.9


Install
===============

```bash

python -m pip install -r requirements.txt
```

Run
===============
python3 dexy.py

